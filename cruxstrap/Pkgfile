# Description: Utility to install "chroots" with CRUX GNU/Linux distro.
# URL: https://github.com/sp00f1ng/cruxstrap
# Maintainer: Hans Bezemer, hbezemer at kliksafe dot nl
# Depends on: 

name=cruxstrap
version=1.0
release=1
source=(https://github.com/sp00f1ng/${name}/archive/${version}.tar.gz)

build() {
	cd ${name}-${version}
	make DESTDIR=$PKG install
	}

