# Description: 	translates chemicals formula written in ASCII into Unicode.
# URL: 		https://github.com/jwilk/chemiscripts
# Maintainer: 	Hans Bezemer, hbezemer at kliksafe dot nl
# Packager:	Hans Bezemer, hbezemer at kliksafe dot nl
# Depends on: 	

name=chemiscripts
version=1.0
release=1
source=(https://github.com/Allfifthstuning/${name}/archive/refs/tags/${version}.tar.gz)

build() {
	cd ${name}-${version}
	mkdir -pv "$PKG"/usr/bin/
	cp ./chemiscripts "$PKG"/usr/bin/
	}

