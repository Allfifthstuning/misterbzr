# Description:	GNUnet, a framework for secure peer-to-peer networking with the high-level goal to provide a strong foundation for a global, distributed network that provides security and privacy. 
# URL: 		gnunet.org
# Maintainer: 	Hans Bezemer, hbezemer at kliksafe dot nl
# Packager:	Hans Bezemer, hbezemer at kliksafe dot nl
# Depends on:	gnunet, gtk3, glade, xorg-server, xorg-xinit

name=gnunet-gtk
version=0.13.1
release=2
source=(https://mirror.lyrahosting.com/gnu/gnunet/${name}-${version}.tar.gz)

build() {
	cd ${name}-${version}
	./configure PKG_CONFIG_PATH=/usr/lib/pkgconfig/	--prefix=/usr/ 	--disable-documentation --with-sqlite --with-libidn2	
	make DESTDIR=$PKG install
	rm -r $PKG/usr/share/{doc,locale}
	}

