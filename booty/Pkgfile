# Description: Booty is a scripts complex for creating bootable GNU/Linux images
# URL: https://github.com/sp00f1ng/boobstrap
# Maintainer: Hans Bezemer, hbezemer at kliksafe dot nl
# Depends on: cdrkit 

name=booty
version=1.3
release=1
source=(https://github.com/sp00f1ng/${name}/archive/${version}.tar.gz)

build() {
	cd ${name}-${version}
	make DESTDIR=$PKG install
	}

